# Lexical-Analyzer
Lexical Analyzer in C to read the file character by character and categorizes the input into meaningful tokens such as keywords, identifiers, numbers, operators, delimiters, comments, string literals, and preprocessor directives. It also includes parenthesis and braces check.
